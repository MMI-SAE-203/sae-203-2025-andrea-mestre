---
const { ...invite } = Astro.props;
import {formatDate} from "../../backend/backend.mjs"
const date = formatDate(invite.expand?.anime_activite.date_activite);
---

<a href= {`/invités/${invite.id}`} class="bg-white rounded-lg shadow-lg p-4">
    <h2>{invite.nom} {invite.prenom}</h2>
    <p>{invite.metiers}</p>
    <p>{date}</p>
</a>
